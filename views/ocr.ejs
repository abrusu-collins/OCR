<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="bootstrap.min (1).css" />
    <title>Text To Speech</title>
  </head>

  <body>
    <style>
      #textinput,
      .range-group,
      #select {
        width: 40%;
        margin: auto;
        display: block;
      }

      .btn {
        display: block;
        margin: auto;
      }

      #speeddiv,
      #pitchdiv {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
      }

      .table {
        width: 50%;
        margin: auto;
      }

      #historyhead {
        margin-top: 150px;
      }

      #play {
        background: #7162ec;
        text-align: center;
      }

      #deleted {
        background: #dc0c4c;
        text-align: center;
      }

      #play:hover,
      #deleted:hover {
        cursor: pointer;
      }
      #no {
        text-align: center;
      }
      tbody {
        border-bottom: 10px solid rgb(26, 9, 51);
      }
      tr {
        justify-content: center;
      }
      #speakcontrol {
        width: 30%;
        display: flex;
        margin: auto;
      }

      .load img {
        width: 100%;
      }
      #status {
        align-items: center;
        justify-items: center;
        flex-direction: column;
        text-align: center;

        width: 100%;
        height: 100vh;
        position: absolute;
        top: 0;
        left: 0;
        text-align: center;
        display: none;
        align-items: center;
        background-color: rgba(0, 0, 0, 0.7);
      }
      .load {
        padding: 30px;
        border-radius: 5px;
        background-color: #30115e;
        margin: auto;
      }
      #text {
        /* width:40%; */
        text-align: center;
        margin-top: 30px;
        color: #fcfeff;
      }
      .externals {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
      }
      .externals a {
        margin-right: 10px;
      }
      .h3 {
        color: #fcfeff;
        margin-top: 150px;
       
      }
      .invinsible {
        word-break: break-all;
        width: 100%;
        display: none;
        color: #000;
      }
    </style>
    <h1 class="text-center mt-4 mb-5 text-success">Optical Character Recognition</h1>
    <h3 class="h3 text-center mb-5">Chose an Image and Click on Convert</h3>
    <form
      class="form-group"
      method="post"
      action="/uploads"
      enctype="multipart/form-data"
    >
      <input
        type="file"
        class="form-control form-control-lg mt-2"
        id="textinput"
        name="myImage"
      />
      <button type="submit" class="btn btn-outline-primary mt-4" id="convert">
        Convert
      </button>
    </form>

    <div id="status" class="mt-3 mb-3"></div>
    <div id="text"></div>

    <div class="invinsible"></div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.5/jspdf.debug.js"></script>
    <script src="ocr.js"></script>
  </body>
</html>
